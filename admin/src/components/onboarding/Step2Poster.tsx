// STUB — Team Alpha α3 owns this. Step 2: generate invite code + download QR poster.
// Props: { onComplete: () => void; gym: { id: string; name?: string; invite_code?: string }; }
export interface Step2PosterProps {
  onComplete: () => void;
  gym: { id: string; name?: string; invite_code?: string };
}
export function Step2Poster(_props: Step2PosterProps) {
  return null;
}
