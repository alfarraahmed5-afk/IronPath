// STUB — Team Alpha α2 owns this. Step 1: brand the gym (logo + name + accent).
// Props: { onComplete: () => void; gym: { id: string; name?: string; accent_color?: string | null; logo_url?: string | null }; }
export interface Step1BrandProps {
  onComplete: () => void;
  gym: { id: string; name?: string; accent_color?: string | null; logo_url?: string | null };
}
export function Step1Brand(_props: Step1BrandProps) {
  return null;
}
