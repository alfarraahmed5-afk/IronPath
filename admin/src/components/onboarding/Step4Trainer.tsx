// STUB — Team Alpha α4 owns this. Step 4 (optional): invite a trainer.
// Props: { onComplete: () => void; onSkip: () => void; gym: { id: string }; }
export interface Step4TrainerProps {
  onComplete: () => void;
  onSkip: () => void;
  gym: { id: string };
}
export function Step4Trainer(_props: Step4TrainerProps) {
  return null;
}
