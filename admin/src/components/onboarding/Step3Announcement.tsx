// STUB — Team Alpha α3 owns this. Step 3: post first announcement.
// Props: { onComplete: () => void; gym: { id: string; name?: string }; }
export interface Step3AnnouncementProps {
  onComplete: () => void;
  gym: { id: string; name?: string };
}
export function Step3Announcement(_props: Step3AnnouncementProps) {
  return null;
}
