// STUB — Team Alpha α4 replaces this with the real Ember Seam:
// 1px horizontal hairline gradient with 12px blur halo, breathing via
// background-position 8s. Used along the bottom edge of every page header
// and the top edge of every hero/photo band.
export function EmberSeam({ className = '' }: { className?: string }) {
  return <div className={className} aria-hidden="true" />;
}
