/**
 * Legacy Button -- shim re-exporting the design-system Button.
 *
 * The legacy export keeps the API name stable for existing call sites.
 */
export { Button } from '../design-system/primitives/Button';
export type { ButtonProps, ButtonVariant, ButtonSize } from '../design-system/primitives/Button';
