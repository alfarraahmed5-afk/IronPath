/**
 * Skeleton -- legacy import path. Now re-exports the design-system
 * primitive (Team B-2 PR B rewrite). Skia LinearGradient shimmer,
 * variants block/line/circle/tile/card, reduce-motion aware.
 */
export { Skeleton, type SkeletonProps, type SkeletonVariant } from '../design-system/primitives/Skeleton';
