/**
 * STUB -- replaced by Team A in PR A.
 *
 * Placeholder re-export of the legacy `mobile/src/theme/tokens.ts`
 * `colors` object. Team A (lens 2's spec) replaces this with the full
 * crimson palette + warm-ink scale + semantic colors.
 *
 * Public API: any module may import { colors } from this path and
 * expect a stable shape across the migration.
 */
export { colors } from '../../theme/tokens';
