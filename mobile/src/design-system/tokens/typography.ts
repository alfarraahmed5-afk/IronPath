/**
 * STUB -- replaced by Team A in PR A.
 *
 * Placeholder re-export of the legacy type scale. Team A replaces with
 * Mona Sans + IBM Plex Sans Arabic + Barlow body + JetBrains Mono per
 * lens 2's typography spec.
 */
export { type as typography } from '../../theme/tokens';
