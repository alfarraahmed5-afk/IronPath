// STUB — Team Alpha α1 owns the real Cold Open scene (0-100vh).
// Hero with AVIF chalk-hands LCP poster, headline, sub-head, primary CTA.
// EmberCanvas mounts here AFTER LCP.

export default function ColdOpenScene() {
  return (
    <section className="min-h-[100svh] grid place-items-center px-4">
      <div className="text-center max-w-2xl">
        <p className="font-mono text-xs text-ink-400 mb-2">Cold Open</p>
        <h1 className="font-display text-4xl sm:text-6xl mb-4">
          Run your gym, not software.
        </h1>
        <p className="text-ink-300 max-w-md mx-auto">α1 in flight</p>
      </div>
    </section>
  );
}
